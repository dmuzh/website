/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: [
            "icons-for-free.com",
            "encrypted-tbn0.gstatic.com",

        ],
    },
}

module.exports = nextConfig